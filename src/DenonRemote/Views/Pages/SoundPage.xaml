<pages:PageBase
    x:Class="DenonRemote.Views.Pages.SoundPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:pages="clr-namespace:DenonRemote.Views.Pages"
    xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
    mc:Ignorable="d">

    <pages:PageBase.Resources>
        <Style x:Key="PillBtn" TargetType="Button">
            <Setter Property="Background" Value="{StaticResource Surface2Brush}" />
            <Setter Property="Foreground" Value="{StaticResource TextPrimaryBrush}" />
            <Setter Property="BorderBrush" Value="{StaticResource CardBorderBrush}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Padding" Value="11,6" />
            <Setter Property="FontSize" Value="12" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border
                            x:Name="Root"
                            Padding="{TemplateBinding Padding}"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="14">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Root" Property="BorderBrush" Value="{StaticResource AccentBrush}" />
                                <Setter TargetName="Root" Property="Background" Value="{StaticResource Surface1Brush}" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </pages:PageBase.Resources>

    <Grid Margin="20,16,20,16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0" Margin="0,0,0,14">
            <TextBlock FontSize="26" FontWeight="Bold" Foreground="{StaticResource TextPrimaryBrush}" Text="Sonido" />
            <TextBlock Style="{StaticResource Subtitle}" Text="Nivel por canal, tono, subwoofer y perfiles." />
        </StackPanel>

        <!-- Profiles bar -->
        <Border Grid.Row="1" Margin="0,0,0,12" Padding="16,12" Style="{StaticResource Card}">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <TextBlock
                    VerticalAlignment="Center"
                    FontSize="13"
                    FontWeight="SemiBold"
                    Foreground="{StaticResource TextSecondaryBrush}"
                    Text="PERFILES" />

                <!-- Saved profile chips -->
                <ItemsControl Grid.Column="1" Margin="14,0" VerticalAlignment="Center" ItemsSource="{Binding Profiles}">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <WrapPanel Orientation="Horizontal" />
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border
                                Margin="0,0,6,0"
                                Background="{StaticResource Surface2Brush}"
                                BorderBrush="{StaticResource CardBorderBrush}"
                                BorderThickness="1"
                                CornerRadius="14">
                                <StackPanel Orientation="Horizontal">
                                    <!-- Click loads the profile; double-click switches to rename -->
                                    <Button
                                        Padding="12,6"
                                        Background="Transparent"
                                        BorderThickness="0"
                                        Click="OnLoadProfile"
                                        Cursor="Hand"
                                        FontSize="12"
                                        FontWeight="SemiBold"
                                        Foreground="{StaticResource TextPrimaryBrush}"
                                        MouseDoubleClick="OnRenameProfile"
                                        Tag="{Binding}"
                                        ToolTip="Click para cargar, doble click para renombrar">
                                        <Grid>
                                            <TextBlock x:Name="NameText" Text="{Binding Name}" />
                                            <TextBox
                                                x:Name="NameEdit"
                                                MinWidth="70"
                                                Padding="0"
                                                Background="Transparent"
                                                BorderThickness="0"
                                                FontSize="12"
                                                FontWeight="SemiBold"
                                                Foreground="{StaticResource TextPrimaryBrush}"
                                                KeyDown="OnRenameKeyDown"
                                                LostFocus="OnRenameCommit"
                                                Tag="{Binding}"
                                                Text="{Binding Name, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                                Visibility="Collapsed" />
                                        </Grid>
                                    </Button>
                                    <Button
                                        Padding="4,6,8,6"
                                        Background="Transparent"
                                        BorderThickness="0"
                                        Click="OnDeleteProfile"
                                        Content="✕"
                                        Cursor="Hand"
                                        FontSize="10"
                                        Foreground="{StaticResource TextTertiaryBrush}"
                                        Tag="{Binding}"
                                        ToolTip="Eliminar" />
                                </StackPanel>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>

                <StackPanel Grid.Column="2" Orientation="Horizontal" VerticalAlignment="Center">
                    <Button
                        Margin="0,0,6,0"
                        Command="{Binding NormalizeChannelsCommand}"
                        Content="Normalizar"
                        Style="{StaticResource PillBtn}"
                        ToolTip="Todos los canales a 0 dB" />
                    <Button
                        Command="{Binding SaveProfileCommand}"
                        Content="+ Guardar"
                        Style="{StaticResource PillBtn}"
                        ToolTip="Guardar niveles actuales" />
                </StackPanel>
            </Grid>
        </Border>

        <!-- Two-column layout: channels on the left, tone + subwoofer on the right -->
        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="380" />
            </Grid.ColumnDefinitions>

            <!-- Channel levels -->
            <Border Grid.Column="0" Margin="0,0,6,0" Padding="18" Style="{StaticResource Card}">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>

                    <StackPanel Grid.Row="0" Margin="0,0,0,10">
                        <TextBlock FontSize="17" FontWeight="SemiBold" Foreground="{StaticResource TextPrimaryBrush}" Text="Nivel por canal" />
                        <TextBlock FontSize="12" Foreground="{StaticResource TextTertiaryBrush}" Text="Ajuste fino de cada altavoz (-12 a +12 dB)" />
                    </StackPanel>

                    <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
                        <StackPanel>
                            <ItemsControl ItemsSource="{Binding ActiveChannels}">
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <Grid Margin="0,5">
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="52" />
                                                <ColumnDefinition Width="*" />
                                                <ColumnDefinition Width="62" />
                                            </Grid.ColumnDefinitions>
                                            <Border
                                                Width="44"
                                                Height="24"
                                                VerticalAlignment="Center"
                                                Background="{Binding IsSpeakerActive, Converter={StaticResource ChannelBg}}"
                                                CornerRadius="5"
                                                ToolTip="Rojo = reproduciendo en el modo actual">
                                                <TextBlock
                                                    HorizontalAlignment="Center"
                                                    VerticalAlignment="Center"
                                                    FontSize="11"
                                                    FontWeight="Bold"
                                                    Foreground="{Binding IsSpeakerActive, Converter={StaticResource ChannelFg}}"
                                                    Text="{Binding Label}" />
                                            </Border>
                                            <Slider
                                                Grid.Column="1"
                                                Margin="8,0"
                                                VerticalAlignment="Center"
                                                IsMoveToPointEnabled="True"
                                                LargeChange="0.5"
                                                Maximum="12"
                                                Minimum="-12"
                                                SmallChange="0.5"
                                                Tag="{Binding Label}"
                                                TickFrequency="3"
                                                ValueChanged="OnChannelLevelChanged"
                                                Value="{Binding LevelDb, Mode=TwoWay}" />
                                            <TextBlock
                                                Grid.Column="2"
                                                HorizontalAlignment="Right"
                                                VerticalAlignment="Center"
                                                FontSize="13"
                                                FontWeight="SemiBold"
                                                Foreground="{StaticResource TextPrimaryBrush}"
                                                Text="{Binding LevelDb, StringFormat='{}{0:+0.#;-0.#;0} dB'}" />
                                        </Grid>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>

                            <TextBlock
                                Margin="0,10,0,0"
                                FontSize="12"
                                Foreground="{StaticResource TextTertiaryBrush}"
                                Text="Conecta el receptor para ver los canales activos."
                                TextWrapping="Wrap"
                                Visibility="{Binding ActiveChannels.Count, Converter={StaticResource ZeroToVisible}}" />
                        </StackPanel>
                    </ScrollViewer>
                </Grid>
            </Border>

            <!-- Right column: tone + subwoofer -->
            <StackPanel Grid.Column="1" Margin="6,0,0,0">
                <Border Margin="0,0,0,12" Padding="18" Style="{StaticResource Card}">
                    <StackPanel>
                        <Grid Margin="0,0,0,10">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>
                            <TextBlock FontSize="17" FontWeight="SemiBold" Foreground="{StaticResource TextPrimaryBrush}" Text="Control de tono" />
                            <ui:ToggleSwitch Grid.Column="1" IsChecked="{Binding State.ToneControlEnabled, Mode=TwoWay}" />
                        </Grid>

                        <TextBlock FontSize="11" Foreground="{StaticResource TextTertiaryBrush}" Text="GRAVES" />
                        <Grid Margin="0,0,0,10">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="58" />
                            </Grid.ColumnDefinitions>
                            <Slider
                                IsEnabled="{Binding State.ToneControlEnabled}"
                                LargeChange="1"
                                Maximum="6"
                                Minimum="-6"
                                SmallChange="1"
                                TickFrequency="1"
                                Value="{Binding State.Bass, Mode=TwoWay, Delay=200}" />
                            <TextBlock
                                Grid.Column="1"
                                HorizontalAlignment="Right"
                                VerticalAlignment="Center"
                                FontSize="13"
                                FontWeight="SemiBold"
                                Text="{Binding State.Bass, StringFormat='{}{0:+0;-0;0} dB'}" />
                        </Grid>

                        <TextBlock FontSize="11" Foreground="{StaticResource TextTertiaryBrush}" Text="AGUDOS" />
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="58" />
                            </Grid.ColumnDefinitions>
                            <Slider
                                IsEnabled="{Binding State.ToneControlEnabled}"
                                LargeChange="1"
                                Maximum="6"
                                Minimum="-6"
                                SmallChange="1"
                                TickFrequency="1"
                                Value="{Binding State.Treble, Mode=TwoWay, Delay=200}" />
                            <TextBlock
                                Grid.Column="1"
                                HorizontalAlignment="Right"
                                VerticalAlignment="Center"
                                FontSize="13"
                                FontWeight="SemiBold"
                                Text="{Binding State.Treble, StringFormat='{}{0:+0;-0;0} dB'}" />
                        </Grid>
                    </StackPanel>
                </Border>

                <Border Margin="0,0,0,12" Padding="18" Style="{StaticResource Card}">
                    <StackPanel>
                        <TextBlock FontSize="17" FontWeight="SemiBold" Foreground="{StaticResource TextPrimaryBrush}" Text="Subwoofer" />
                        <TextBlock Margin="0,2,0,10" FontSize="12" Foreground="{StaticResource TextTertiaryBrush}" Text="Salida del subwoofer (pasos de 0.5 dB)" />
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="58" />
                            </Grid.ColumnDefinitions>
                            <Slider
                                LargeChange="0.5"
                                Maximum="12"
                                Minimum="-12"
                                SmallChange="0.5"
                                TickFrequency="3"
                                Value="{Binding State.SubwooferLevel, Mode=TwoWay, Delay=200}" />
                            <TextBlock
                                Grid.Column="1"
                                HorizontalAlignment="Right"
                                VerticalAlignment="Center"
                                FontSize="13"
                                FontWeight="SemiBold"
                                Text="{Binding State.SubwooferLevel, StringFormat='{}{0:+0.#;-0.#;0} dB'}" />
                        </Grid>
                    </StackPanel>
                </Border>

                <!-- ── Zones (moved here from its own tab) ─────────────────── -->
                <Border Padding="18" Style="{StaticResource Card}">
                    <StackPanel>
                        <TextBlock FontSize="17" FontWeight="SemiBold" Foreground="{StaticResource TextPrimaryBrush}" Text="Zonas" />
                        <TextBlock Margin="0,2,0,12" FontSize="12" Foreground="{StaticResource TextTertiaryBrush}" Text="Audio independiente para otras habitaciones" />

                        <!-- Main zone -->
                        <Grid Margin="0,0,0,6">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="46" />
                            </Grid.ColumnDefinitions>
                            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                <Ellipse
                                    Width="8"
                                    Height="8"
                                    VerticalAlignment="Center"
                                    Fill="{Binding State.Main.IsOn, Converter={StaticResource ChannelBg}}" />
                                <TextBlock
                                    Margin="6,0,8,0"
                                    VerticalAlignment="Center"
                                    FontSize="12"
                                    FontWeight="SemiBold"
                                    Foreground="{StaticResource TextPrimaryBrush}"
                                    Text="Principal" />
                            </StackPanel>
                            <Slider
                                Grid.Column="1"
                                VerticalAlignment="Center"
                                IsEnabled="{Binding State.Main.IsOn}"
                                LargeChange="1"
                                Maximum="150"
                                Minimum="0"
                                SmallChange="0.5"
                                Value="{Binding State.Main.Volume, Mode=TwoWay, Delay=250}" />
                            <TextBlock
                                Grid.Column="2"
                                HorizontalAlignment="Right"
                                VerticalAlignment="Center"
                                FontSize="12"
                                FontWeight="SemiBold"
                                Foreground="{StaticResource TextPrimaryBrush}"
                                Text="{Binding State.Main.Volume, Converter={StaticResource VolumeLabel}}" />
                        </Grid>

                        <!-- Zone 2 -->
                        <Grid Margin="0,6,0,0">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="46" />
                            </Grid.ColumnDefinitions>
                            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                <Ellipse
                                    Width="8"
                                    Height="8"
                                    VerticalAlignment="Center"
                                    Fill="{Binding State.Zone2.IsOn, Converter={StaticResource ChannelBg}}" />
                                <TextBlock
                                    Margin="6,0,8,0"
                                    VerticalAlignment="Center"
                                    FontSize="12"
                                    FontWeight="SemiBold"
                                    Foreground="{StaticResource TextPrimaryBrush}"
                                    Text="Zona 2" />
                            </StackPanel>
                            <Slider
                                Grid.Column="1"
                                VerticalAlignment="Center"
                                IsEnabled="{Binding State.Zone2.IsOn}"
                                LargeChange="1"
                                Maximum="98"
                                Minimum="0"
                                SmallChange="0.5"
                                Value="{Binding State.Zone2.Volume, Mode=TwoWay, Delay=250}" />
                            <TextBlock
                                Grid.Column="2"
                                HorizontalAlignment="Right"
                                VerticalAlignment="Center"
                                FontSize="12"
                                FontWeight="SemiBold"
                                Foreground="{StaticResource TextPrimaryBrush}"
                                Text="{Binding State.Zone2.Volume, Converter={StaticResource VolumeLabel}}" />
                        </Grid>

                        <Button
                            Margin="0,10,0,0"
                            HorizontalAlignment="Stretch"
                            Command="{Binding ToggleZone2Command}"
                            Content="Encender / Apagar Zona 2"
                            Style="{StaticResource PillBtn}" />
                        <TextBlock
                            Margin="0,6,0,0"
                            FontSize="10"
                            Foreground="{StaticResource TextTertiaryBrush}"
                            Text="Zona 2 se enciende solo manualmente y se apaga con la principal."
                            TextWrapping="Wrap" />
                    </StackPanel>
                </Border>
            </StackPanel>
        </Grid>
    </Grid>
</pages:PageBase>
